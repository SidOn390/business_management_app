import 'package:flutter/material.dart';

class ReceiptEntryScreen extends StatelessWidget {
  const ReceiptEntryScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Dashboard')));
  }
}
